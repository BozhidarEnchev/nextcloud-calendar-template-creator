[project]
name = "calendar-event-template-creator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "jinja2",
    "python-multipart",
    "sqlalchemy",
    "caldav",
    "icalendar",
    "argon2-cffi",
    "cryptography",
    "pydantic-settings",
    "itsdangerous",
]

[dependency-groups]
dev = [
    "pytest",
    "httpx",
]
